/**
 * Set Active Session Id
 */
const setActiveSessionId = (state, _sessionId) => {
    /* Set active session id. */
    state.activeSessionId = _sessionId
}

/* Export module. */
export default setActiveSessionId
